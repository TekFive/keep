rootProject.name = "keep"

// Standalone build: sibling libs (jfk, ack, kviash) are resolved as published org.tekfive:*
// artifacts from GitHub Packages (see build.gradle.kts). Full-source development happens in the
// AIDEWAY umbrella build, which includes all the libs together.
